spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpashop
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#       show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug #sql문 log 남기기
    org.hibernate.type: trace #sql 파라미터 log 남기기

